workflows:
  android_workflow:
    name: Build Android APK
    environment:
      flutter: 3.24.0
    scripts:
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
